# Docker Module

# Author: Víctor Barceló

## Overview

This module introduces Docker, a platform for developing, shipping, and running applications in isolated environments called containers. Docker simplifies dependency management, ensures environment consistency across development and production, and is a fundamental tool in modern software engineering workflows.

## What is Docker?

Docker is an open-source platform that packages applications and their dependencies into lightweight, portable units called **containers**. Unlike virtual machines, containers share the host operating system kernel, making them faster to start, less resource-intensive, and easier to distribute.

### Key Concepts

**Container**
A runnable instance of an image. Containers are isolated from each other and from the host system, but can be connected through defined interfaces (ports, volumes, networks).

**Image**
A read-only template used to create containers. Images are built from a `Dockerfile` and can be stored in registries such as Docker Hub or a private registry.

**Dockerfile**
A text file containing a sequence of instructions that Docker uses to build an image. Each instruction creates a layer in the image.

**Registry**
A storage and distribution system for Docker images. Docker Hub is the default public registry. Organizations often use private registries for internal images.

**Volume**
A mechanism for persisting data generated by and used in Docker containers. Volumes are managed by Docker and exist outside the container lifecycle.

**Network**
Docker networks allow containers to communicate with each other and with the outside world. The default bridge network is created automatically; custom networks give more control.

## Why Use Docker?

- **Consistency**: The same container runs identically on a developer laptop, a CI server, and production.
- **Isolation**: Applications and their dependencies do not interfere with each other or the host system.
- **Portability**: Containers can run on any machine with a Docker runtime, regardless of the underlying OS.
- **Speed**: Containers start in seconds compared to minutes for virtual machines.
- **Scalability**: Container orchestration tools (Docker Compose, Kubernetes) make scaling straightforward.

## Docker vs Virtual Machines

| Feature           | Docker Container        | Virtual Machine         |
|-------------------|------------------------|-------------------------|
| Boot time         | Seconds                | Minutes                 |
| Size              | Megabytes              | Gigabytes               |
| OS overhead       | None (shared kernel)   | Full OS per VM          |
| Isolation         | Process-level          | Hardware-level          |
| Portability       | High                   | Medium                  |
| Resource usage    | Low                    | High                    |

## Module Contents

### 1. [Linux Installation](install_linux.md)
Step-by-step guide to installing Docker Engine and Docker Compose on Debian/Ubuntu and RHEL/Fedora-based distributions.

### 2. [Windows Installation](install_windows.md)
Step-by-step guide to installing Docker Desktop on Windows 10/11, including WSL 2 backend configuration.

## Basic Usage

Once Docker is installed, the following commands cover most day-to-day tasks:

```bash
# Verify installation
docker --version
docker compose version

# Pull an image from Docker Hub
docker pull ubuntu:24.04

# Run a container interactively
docker run -it ubuntu:24.04 bash

# List running containers
docker ps

# List all containers (including stopped)
docker ps -a

# Stop a container
docker stop <container_id>

# Remove a container
docker rm <container_id>

# List images
docker images

# Remove an image
docker rmi <image_id>
```

## Learning Objectives

By the end of this module, students will be able to:

- Explain what Docker is and describe the difference between containers and virtual machines
- Install Docker on Linux and Windows environments
- Pull images from Docker Hub and run containers
- Understand the purpose of Dockerfiles, volumes, and networks
- Use Docker Compose to define and run multi-container applications
